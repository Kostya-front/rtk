// Здесь приводятся типы всех тех сущностей, с которыми мы можем работать,
// в частности, работая с юзерами, логично описать юзера)
export interface IUser {
    id: number
    name: string
    email: string
    username: string
}